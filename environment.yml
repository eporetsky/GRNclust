name: robustgrn
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.9
  - pip
  - clusterone=1.0
  - pip:
      - click
      - mutclust
      - pyrra         # grn.batch.py: PyRRA rank aggregation
      - statsmodels  # grn.batch.py: per-TF FDR (multipletests fdr_bh)
      #- -e .
